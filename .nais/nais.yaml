apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: biljard
  namespace: bussfaktor
spec:
  image: "{{ image }}"
  port: 8080
  liveness:
    path: /api/internal/isAlive
    initialDelay: 10
  readiness:
    path: /api/internal/isReady
    initialDelay: 10
  replicas:
    min: 1
    max: 2
  resources:
    limits:
      memory: 512Mi
    requests:
      cpu: 50m
      memory: 256Mi
  azure:
    application:
      enabled: true
      allowAllUsers: true
      claims:
        groups:
          - id: "95c8e577-168f-4c03-b14e-658947a94f3a" 
        extra:
          - NAVident
    sidecar:
      enabled: true
      autoLogin: true
      autoLoginIgnorePaths:
        - /api/internal/*
  env:
    - name: ADMIN_GROUP_ID
      value: "95c8e577-168f-4c03-b14e-658947a94f3a" 
  filesFrom:
    - persistentVolumeClaim: biljard-data
      mountPath: /app/data
  ingresses:
    - https://biljard.nav.no
